# modcompat_strict 场景配置档案（与 modcompat 同档；场景名不同故需独立文件）。
# seedGenEnabled=false：避免本地生成柱把 networkReceived 驻留口径淹没
# （历史 readyNotApplied 假阳性；与 classic 档复位口径一致）。
chunk.seedGenEnabled = false
debug.networkMetricsEnabled = true
